# Summer Cursor rules (project copy)

This folder does **not** replace Cursor’s built-in rules UI. It is a **project-local copy** of guidance that matches how we have been working on this site (React + Vite, `src/data`, `public` assets, copy updates).

There were **no** `.cursor/rules` files in this repository when this folder was created; the originals live in your **Cursor user rules** (and any global/project rules you configure in Cursor). Use this folder as a checklist for agents and humans working in this codebase.

See `frontend-and-content.md` for the distilled rules.
